import {

} from '../types/index';

const initialState = {
  favoritesMovies : []
}

export default function(state = initialState, action) {
  switch(action.type) {
    default:
      return state
  }
}